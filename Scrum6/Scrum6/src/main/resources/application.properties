# =====================================================
# CONFIGURACIÓN GENERAL DE LA APLICACIÓN
# =====================================================
spring.application.name=Scrum6
server.port=${PORT:${SERVER_PORT:8082}}

# Clave fija JWT - evita invalidar tokens al reiniciar la app
jwt.secret=${JWT_SECRET:EcoSoftware2026#SecretKeyJWT_MustBe32CharsMin!!}

spring.main.lazy-initialization=true

# =====================================================
# BASE DE DATOS - POSTGRESQL (Azure)
# =====================================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ecosoftware-postgres.postgres.database.azure.com:5432/postgres?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ecoAdmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:SofistaSRC@26}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER:org.postgresql.Driver}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# =====================================================
# CONFIGURACIÓN DE GEOCODING
# =====================================================
app.geocoding.nominatim-url=${GEOCODING_NOMINATIM_URL:https://nominatim.openstreetmap.org/search}
app.geocoding.mapsco-url=${GEOCODING_MAPSCO_URL:https://geocode.maps.co/search}
app.geocoding.user-agent=${GEOCODING_USER_AGENT:EcoSoftwareScrum6/1.0 (ecosoftwaresrc@gmail.com)}
app.geocoding.email=${GEOCODING_EMAIL:ecosoftwaresrc@gmail.com}

# =====================================================
# CONFIGURACIÓN CORS
# =====================================================
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://ecosoftware.azurewebsites.net}

# =====================================================
# API EXTERNA - GNEWS
# =====================================================
gnews.api.url=${GNEWS_API_URL:https://gnews.io/api/v4/search}
gnews.api.key=${GNEWS_API_KEY:b45cd7e4114c52d97c0a2dc345763beb}

# =====================================================
# CLOUDINARY
# =====================================================
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:dhl2ixdsr}
cloudinary.api_key=${CLOUDINARY_API_KEY:934953395445375}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:gQ4beD0zfD2XgOQcD23jJ1cwjZI}

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# =====================================================
# CONFIGURACIÓN DE CORREO (GMAIL SMTP)
# =====================================================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:ecosoftwaresrc@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gwgv zjtk ztij jfys}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST}